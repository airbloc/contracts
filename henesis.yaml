version: v1
name: airbloc

contract:
  address: '0x'
  path: ./contracts/xxx.sol
  name: ERC20
  compilerVersion: 0.5.10

blockchain:
  platform: ethereum
  network: ropsten

handlers:
  # dai-transfer:
  #   event: Transfer(address,address,uint256)

  # dai-approval:
  #   event: Approval(address,address,uint256)

webhook:
  url: https://localhost:3000
  method: POST
  # headers:
  #   Authorization: 'Bearer YOUR-OWN-TOKEN'
